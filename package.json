{
  "name": "budget-deploy",
  "private": true,
  "scripts": {
    "start": "npm --prefix backend start",
    "dev": "npm run dev --prefix frontend",
    "dev:full": "concurrently -n backend,frontend -c blue,green \"npm run start --prefix backend\" \"npm run dev --prefix frontend\"",
    "build:local": "npm run build --prefix frontend && node backend/scripts/stage-frontend.mjs",
    "start:local": "npm run build:local && npm run start --prefix backend",
    "postinstall": "npm --prefix backend install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
